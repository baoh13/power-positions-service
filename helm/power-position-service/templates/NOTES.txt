Power Position Service has been deployed!

Release Name: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Replicas: {{ .Values.replicaCount }}

To check the status of your deployment:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "power-position-service.name" . }}

To view logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "power-position-service.name" . }} -f

Storage Configuration:
  Output Directory: /app/data/output {{ if .Values.storage.output.hostPath }}(mounted from {{ .Values.storage.output.hostPath }}){{ end }}
  Audit Directory: /app/data/audit {{ if .Values.storage.audit.hostPath }}(mounted from {{ .Values.storage.audit.hostPath }}){{ end }}
  DLQ Directory: /app/data/dlq {{ if .Values.storage.dlq.hostPath }}(mounted from {{ .Values.storage.dlq.hostPath }}){{ end }}

Extraction Interval: Every {{ .Values.powerPositionSettings.intervalMinutes }} minutes

{{- if .Values.env.DOTNET_RUNTIME }}
⚠️  ADHOC RERUN MODE ENABLED
Runtime Override: {{ .Values.env.DOTNET_RUNTIME }}
{{- end }}

For production operations, see the deployment guide.
