# Generated by GitHub Copilot on December 13, 2025
{{- if and .Values.storage.enabled (not .Values.storage.output.hostPath) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "power-position-service.fullname" . }}-output
  labels:
    {{- include "power-position-service.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.storage.output.size }}
---
{{- end }}
{{- if and .Values.storage.enabled (not .Values.storage.audit.hostPath) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "power-position-service.fullname" . }}-audit
  labels:
    {{- include "power-position-service.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.storage.audit.size }}
---
{{- end }}
{{- if and .Values.storage.enabled (not .Values.storage.dlq.hostPath) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "power-position-service.fullname" . }}-dlq
  labels:
    {{- include "power-position-service.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.storage.dlq.size }}
{{- end }}
