# Generated by GitHub Copilot on December 13, 2025
# Development environment values
replicaCount: 1

image:
  repository: ghcr.io/baoh13/power-position-service
  tag: "dev-latest"
  pullPolicy: Always

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

powerPositionSettings:
  intervalMinutes: 1
  timeZoneId: "Europe/London"
  retryAttempts: 2
  retryDelaySeconds: 10
  maxDlqRetryAttempts: 5

storage:
  storageClassName: "local-path"
  output:
    size: 5Gi
    hostPath: "/mnt/power-reports-dev/output"
  audit:
    size: 2Gi
    hostPath: "/mnt/power-reports-dev/audit"
  dlq:
    size: 1Gi
    hostPath: "/mnt/power-reports-dev/dlq"

logging:
  level: "Debug"

# Dev-specific node selector (if you have dedicated dev nodes)
nodeSelector:
  environment: dev
