# Generated by GitHub Copilot on December 13, 2025
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "power-position-service.fullname" . }}
  labels:
    {{- include "power-position-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "power-position-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "power-position-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "power-position-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: PowerPositionSettings__IntervalMinutes
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: intervalMinutes
        - name: PowerPositionSettings__TimeZoneId
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: timeZoneId
        - name: PowerPositionSettings__OutputDirectory
          value: "/app/data/output"
        - name: PowerPositionSettings__AuditDirectory
          value: "/app/data/audit"
        - name: PowerPositionSettings__DlqDirectory
          value: "/app/data/dlq"
        - name: PowerPositionSettings__RetryAttempts
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: retryAttempts
        - name: PowerPositionSettings__RetryDelaySeconds
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: retryDelaySeconds
        - name: PowerPositionSettings__MaxDlqRetryAttempts
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: maxDlqRetryAttempts
        - name: Logging__LogLevel__Default
          valueFrom:
            configMapKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: loggingLevel
        {{- if .Values.env.POWER_POSITION_RUNTIME }}
        - name: DOTNET_RUNTIME
          value: {{ .Values.env.POWER_POSITION_RUNTIME | quote }}
        {{- end }}
        # Secret environment variables
        - name: PowerService__ApiKey
          valueFrom:
            secretKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: powerServiceApiKey
        - name: PowerService__Username
          valueFrom:
            secretKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: powerServiceUsername
              optional: true
        - name: PowerService__Password
          valueFrom:
            secretKeyRef:
              name: {{ include "power-position-service.fullname" . }}
              key: powerServicePassword
              optional: true
        volumeMounts:
        - name: output-data
          mountPath: /app/data/output
        - name: audit-data
          mountPath: /app/data/audit
        - name: dlq-data
          mountPath: /app/data/dlq
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        {{- if .Values.livenessProbe.enabled }}
        livenessProbe:
          {{- toYaml .Values.livenessProbe.httpGet | nindent 10 }}
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
        {{- end }}
        {{- if .Values.readinessProbe.enabled }}
        readinessProbe:
          {{- toYaml .Values.readinessProbe.httpGet | nindent 10 }}
          initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
        {{- end }}
      volumes:
      - name: output-data
        {{- if .Values.storage.output.hostPath }}
        hostPath:
          path: {{ .Values.storage.output.hostPath }}
          type: DirectoryOrCreate
        {{- else }}
        persistentVolumeClaim:
          claimName: {{ include "power-position-service.fullname" . }}-output
        {{- end }}
      - name: audit-data
        {{- if .Values.storage.audit.hostPath }}
        hostPath:
          path: {{ .Values.storage.audit.hostPath }}
          type: DirectoryOrCreate
        {{- else }}
        persistentVolumeClaim:
          claimName: {{ include "power-position-service.fullname" . }}-audit
        {{- end }}
      - name: dlq-data
        {{- if .Values.storage.dlq.hostPath }}
        hostPath:
          path: {{ .Values.storage.dlq.hostPath }}
          type: DirectoryOrCreate
        {{- else }}
        persistentVolumeClaim:
          claimName: {{ include "power-position-service.fullname" . }}-dlq
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
